<script setup lang="ts">
// const { t } = useI18n()
</script>

<template>
  <div class="bg-white flex flex-col min-h-full pt-16 pb-12 antialiased">
    <router-view />
    <!-- <footer class="mx-auto flex-shrink-0 w-full max-w-7xl px-4 sm:px-6 lg:px-8">
      <nav class="flex space-x-4 justify-center">
        <a
          href="#"
          class="font-medium text-sm text-gray-500 hover:text-gray-600"
          >{{ t('not-found.text.links.support') }}</a
        >
        <span
          class="border-l border-gray-300 inline-block"
          aria-hidden="true"
        />
        <a
          href="#"
          class="font-medium text-sm text-gray-500 hover:text-gray-600"
          >{{ t('not-found.text.links.status') }}</a
        >
        <span
          class="border-l border-gray-300 inline-block"
          aria-hidden="true"
        />
        <a
          href="#"
          class="font-medium text-sm text-gray-500 hover:text-gray-600"
          >{{ t('not-found.text.links.social') }}</a
        >
      </nav>
    </footer> -->
  </div>
</template>
